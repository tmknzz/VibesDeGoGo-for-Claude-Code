# VibesDeGoGo! self-maintenance (dogfooding) state — not part of the distribution.
# The skill writes these into whatever repo it runs in; when we run VDGG on this
# repo itself, keep the session state and working artifacts out of commits.
.claude/.vdgg-*
tasks/vdgg/
